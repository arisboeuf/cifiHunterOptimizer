# cifi-tools WASM (Borge)

Contains `release.wasm` from https://cifi-tools.com/wasm/release.wasm — the same
binary used by the online Borge simulator (`EVALBORGE_WASM`).

Refresh with:

```
python scripts/fetch_cifi_wasm.py
```

Requires the `wasmtime` Python package to run.
